services:
  postgres:
    image: postgres:16
    container_name: stablecoin_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: stablecoin_db
    ports:
      - "5432:5432"
    volumes:
      - ./db:/var/lib/postgresql/data
